{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ESP-IDF: Flash and Monitor",
            "type": "espidf",
            "request": "launch",
            "program": "${workspaceFolder}/build/spin_rx.elf",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}"
        }
    ]
}